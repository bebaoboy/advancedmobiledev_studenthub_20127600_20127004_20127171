class NetworkConstants {
  NetworkConstants._();

  // base url
  static const String baseUrl = 'http://jsonplaceholder.typicode.com';

  // receiveTimeout
  static const int receiveTimeout = 4000;

  // connectTimeout
  static const int connectionTimeout = 3000;
}